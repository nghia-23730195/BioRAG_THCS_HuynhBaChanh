"""Test utilities for the ETL pipeline.

Scripts in this folder run the production ImageProcessor / loaders on a
single PDF page and dump every intermediate output for visual QA, without
touching the live status DB or vector store.
"""
